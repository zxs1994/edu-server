<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>dh-framework</artifactId>
        <groupId>cn.dh.oa</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>dh-spring-boot-starter-biz-tenant</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>多租户</description>
    <url>https://gitcode.com/zhouzhongyan/ruoyi-office.git</url>

    <dependencies>
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-common</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- RPC 远程调用相关 -->
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-rpc</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Job 定时任务相关 -->
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-job</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 消息队列相关 -->
        <dependency>
            <groupId>cn.dh.oa</groupId>
            <artifactId>dh-spring-boot-starter-mq</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.amqp</groupId>
            <artifactId>spring-rabbit</artifactId>
            <optional>true</optional>
        </dependency>


        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>

    </dependencies>

</project>
