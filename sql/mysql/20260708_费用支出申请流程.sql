-- ============================================================
-- 费用支出申请 BPM 流程
-- 说明：Flowable 模型含 BPMN 二进制，不建议纯 SQL 硬插。
-- 推荐在【流程管理 -> 流程模型】中复制「日常报销单」模型后修改：
--   1. 标识 Key：oa_expense_payment_bill
--   2. 名称：费用支出申请
--   3. 表单提交路由：/oa/expense-travel/expense-payment-info
--   4. 表单查看路由：/oa/expense-payment/info/index.vue
--   5. 审批节点保持与日常报销单一致（部门负责人 -> 财务 -> 秘书长 -> 抄送会长）
--   6. 点击【发布】生效
--
-- 当前 dev 库日常报销单流程（id=540）节点：
--   发起人 -> 部门负责人 -> 财务(206) -> 秘书长(202) -> 抄送会长(201) -> 结束
-- 无出纳节点，符合需求。
-- ============================================================

-- 若已存在旧模型可先清理（一般无需执行）
-- DELETE FROM act_re_model WHERE KEY_ = 'oa_expense_payment_bill';

-- 已执行标记（在 BPM 后台完成复制发布后取消注释）
-- -- executed: 2026-07-08 费用支出申请流程（BPM 后台发布）
